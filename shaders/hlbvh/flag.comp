#version 450

layout(std430, binding = 4)buffer FlagsBlock { int Flags[];};
uniform int flagLen;

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1)in;

void main(){
    int t = int(gl_GlobalInvocationID . x);
    if(t < flagLen){ Flags[t]= 0;}
}
